---
// src/pages/en/paper/2603.12109/slide.astro
// paper-mode: English slide (horizontal pagination) — 8 Q&A + 4 figures
// v2 aesthetic upgrade: typography +25%, Swiss editorial signature
import BaseLayout from "../../../../layouts/BaseLayout.astro";
import { getEntry } from "astro:content";

const entry = await getEntry("papers", "2603.12109");
if (!entry) throw new Error("paper 2603.12109 not found in collection");
const paper = entry.data as {
  id: string;
  arxiv_id?: string;
  title_zh: string;
  title_en: string;
  title_full_en?: string;
  authors: string[];
  authors_with_affil_en?: { name: string; affil: string[] }[];
  affiliations_en?: string[];
  abstract_zh: string;
  abstract_en: string;
  sections_zh: { heading: string; body: string }[];
  sections_en: { heading: string; body: string }[];
  key_takeaways: string[];
  published_date?: string;
  code_url?: string;
  venue?: string;
  venue_rank?: string;
  venue_url?: string;
  category?: string;
  field_large_zh?: string;
  field_large_en?: string;
  field_medium_zh?: string;
  field_medium_en?: string;
  field_small_zh?: string;
  field_small_en?: string;
  technologies_zh?: string[];
  technologies_en?: string[];
};

const FIG = "/content2html/figures/2603.12109";
const TITLE_EN = paper.title_full_en ?? paper.title_en;
const AUTHORS_EN = paper.authors_with_affil_en ?? paper.authors.map((n) => ({ name: n, affil: [] as string[] }));
const AFFILS_EN: string[] = paper.affiliations_en ?? Array.from(new Set(AUTHORS_EN.flatMap((a) => a.affil)));
const AUTHOR_SUP: string[] = AUTHORS_EN.map((a) =>
  a.affil.map((x) => String(AFFILS_EN.indexOf(x) + 1)).join(",")
);

// 精简 PPT: extract "1./2./3. xxx" bullet titles from body, drop long paragraphs
// Honors "slides should have minimal text" PPT design principle
function extractBullets(body: string): string[] {
  const out: string[] = [];
  // 1) numbered: "1. xxx[:]..." single line
  const numbered = body.match(/^\d+\.\s+[^\n]+/gm);
  if (numbered) out.push(...numbered.map((s) => s.trim()));
  // 2) "Solution overview: xxx" closing segment
  const sol = body.match(/^Solution overview[:\s][^\n]+/gim);
  if (sol) out.push(...sol.map((s) => s.trim()));
  return out;
}

// Strip "Q1." / "Q4.1" prefix from Q&A section headings.
// kicker already labels position (e.g. "Q1 · Problem"), so H2 only needs the question text.
// summary.astro keeps Q-prefix (FAQ enumeration style), only slide.astro strips here.
function cleanHeading(heading: string): string {
  return heading.replace(/^Q\d+(?:\.\d+)?\.\s*/, "");
}
---
<BaseLayout
  title={`${paper.title_en} | paper-slide`}
  description={paper.abstract_en}
  lang="en"
  productType="slide"
>
  <!-- Slide 1: Title (full English title + English authors + English affiliations) -->
  <section class="slide-page active" data-page="1">    <div class="slide-top-accent"></div>
    <div class="slide-meta-bar">
      <span class="meta-page">01 / 13</span>
    </div>

    <div class="col-span-12 flex flex-col justify-center items-center text-center gap-6">
      <hr class="accent-bar" />
      <h1 class="text-headline font-sans font-bold leading-tight max-w-6xl">{TITLE_EN}</h1>
      <hr class="swiss-rule w-40" />
      <p class="text-body leading-relaxed max-w-6xl">
        {AUTHORS_EN.map((a, i) => <><span class="author-name">{a.name}</span><sup class="text-caption text-ikb-blue">{AUTHOR_SUP[i]}</sup>{i < AUTHORS_EN.length - 1 ? " · " : ""}</>)}
      </p>
      <ol class="text-caption text-graphite leading-relaxed list-none max-w-6xl text-left mt-2">
        {AFFILS_EN.map((aff, idx) => <li><sup class="text-ikb-blue font-semibold">{idx + 1}</sup> {aff}</li>)}
      </ol>
    </div>

    <!-- Slide 1 right-bottom metadata corner: arxiv / publish date / code / fields / techs -->
    <aside class="slide-info-corner" aria-label="Paper metadata">
      <dl>
        <dt>arXiv</dt>
        <dd><a href={`https://arxiv.org/abs/${paper.arxiv_id ?? paper.id}`} target="_blank" rel="noopener noreferrer">{paper.arxiv_id ?? paper.id}</a></dd>
        <dt>Published</dt>
        <dd>{paper.published_date ?? "N/A"}</dd>
        <dt>Code</dt>
        <dd>
          {paper.code_url
            ? <a href={paper.code_url} target="_blank" rel="noopener noreferrer">{paper.code_url.replace(/^https?:\/\/(www\.)?/, "").replace(/\/$/, "")}</a>
            : <span>N/A</span>}
        </dd>
        <dt>Venue</dt>
        <dd>
          {paper.venue_url
            ? <a href={paper.venue_url} target="_blank" rel="noopener noreferrer">{paper.venue ?? "—"}</a>
            : <>{paper.venue ?? "—"}</>}
          {paper.venue_rank && <span class="venue-rank">({paper.venue_rank})</span>}
        </dd>
        <dt>Field</dt>
        <dd>{paper.field_large_en ?? "—"} / {paper.field_medium_en ?? "—"} / {paper.field_small_en ?? "—"}</dd>
        <dt>Tech</dt>
        <dd>{(paper.technologies_en ?? []).join(" · ") || "—"}</dd>
      </dl>
    </aside>
  </section>


  <!-- Slide 3: Fig 1 concept — r-2x1 (2.01:1) -->
  <section class="slide-page" data-page="2">    <div class="slide-top-accent"></div>
    <div class="slide-meta-bar">
      <span class="meta-page">02 / 13</span>
    </div>

    <div class="col-span-12 flex flex-col gap-5 h-full">
      <div>
        <p class="kicker mb-3">Figure 1 · Core Diagram</p>
        <hr class="accent-bar mb-6" />
        <h2 class="text-headline font-sans font-bold leading-tight mb-4 max-w-5xl">Information Self-Locking (SeL) and AReW Mitigation</h2>
      </div>
      <p class="caption flex-shrink-0">Top: vanilla outcome-based RL trapped in self-locking (Uninformative AS + Anti-aligned BT); Bottom: AReW restores AS-BT alignment via directional critiques (Informative AS + Aligned BT)</p>
      <figure class="frame-img r-2x1 fit-contain flex-1">
        <img src={`${FIG}/fig1_pipeline.png`} alt="Fig 1: SeL overview + AReW mitigation" />
      </figure>
    </div>
  </section>

  <!-- Slide 4: Q1 -->
  <section class="slide-page" data-page="3">    <div class="slide-top-accent"></div>
    <div class="slide-meta-bar">
      <span class="meta-page">03 / 13</span>
    </div>

    <div class="col-span-12 flex flex-col gap-5 h-full">
      <div>
        <p class="kicker mb-3">Q1 · Problem</p>
        <hr class="accent-bar mb-6" />
        <h2 class="text-headline font-sans font-bold leading-tight max-w-5xl">{cleanHeading(paper.sections_en[0].heading)}</h2>
      </div>
      <ul class="flex-1 flex flex-col gap-2 justify-center max-w-5xl">
        {extractBullets(paper.sections_en[0].body).map((b) => (
          <li class="takeaway-item">{b}</li>
        ))}
      </ul>
    </div>
  </section>

  <!-- Slide 5: Q2 -->
  <section class="slide-page" data-page="4">    <div class="slide-top-accent"></div>
    <div class="slide-meta-bar">
      <span class="meta-page">04 / 13</span>
    </div>

    <div class="col-span-12 flex flex-col gap-5 h-full">
      <div>
        <p class="kicker mb-3">Q2 · Related Work</p>
        <hr class="accent-bar mb-6" />
        <h2 class="text-headline font-sans font-bold leading-tight max-w-5xl">{cleanHeading(paper.sections_en[1].heading)}</h2>
      </div>
      <ul class="flex-1 flex flex-col gap-2 justify-center max-w-5xl">
        {extractBullets(paper.sections_en[1].body).map((b) => (
          <li class="takeaway-item">{b}</li>
        ))}
      </ul>
    </div>
  </section>

  <!-- Slide 6: Q3 -->
  <section class="slide-page" data-page="5">    <div class="slide-top-accent"></div>
    <div class="slide-meta-bar">
      <span class="meta-page">05 / 13</span>
    </div>

    <div class="col-span-12 flex flex-col gap-5 h-full">
      <div>
        <p class="kicker mb-3">Q3 · Method</p>
        <hr class="accent-bar mb-6" />
        <h2 class="text-headline font-sans font-bold leading-tight max-w-5xl">{cleanHeading(paper.sections_en[2].heading)}</h2>
      </div>
      <ul class="flex-1 flex flex-col gap-2 justify-center max-w-5xl">
        {extractBullets(paper.sections_en[2].body).map((b) => (
          <li class="takeaway-item">{b}</li>
        ))}
      </ul>
    </div>
  </section>

  <!-- Slide 7: Fig 4 training dynamics 4 panels — r-16x10 (1.56:1) -->
  <section class="slide-page" data-page="6">    <div class="slide-top-accent"></div>
    <div class="slide-meta-bar">
      <span class="meta-page">06 / 13</span>
    </div>

    <div class="col-span-12 flex flex-col gap-5 h-full">
      <div>
        <p class="kicker mb-3">Figure 4 · Training Dynamics (4 tasks)</p>
        <hr class="accent-bar mb-6" />
        <h2 class="text-headline font-sans font-bold leading-tight mb-4">Training Dynamics Results</h2>
      </div>
      <p class="caption flex-shrink-0">vanilla PPO (red) vs AReW-AS ONLY (green) vs AReW-AS+BT (blue) Reward vs Training Steps; AReW sustained improvement, vanilla stalls</p>
      <figure class="frame-img r-16x10 fit-contain flex-1">
        <img src={`${FIG}/fig4_full.png`} alt="Fig 4: training dynamics" />
      </figure>
    </div>
  </section>

  <!-- Slide 8: Q4 (1/2) -->
  <section class="slide-page" data-page="7">    <div class="slide-top-accent"></div>
    <div class="slide-meta-bar">
      <span class="meta-page">07 / 13</span>
    </div>

    <div class="col-span-12 flex flex-col gap-5 h-full">
      <div>
        <p class="kicker mb-3">Q4.1 · Experiments (Setup + Table 1)</p>
        <hr class="accent-bar mb-6" />
        <h2 class="text-headline font-sans font-bold leading-tight max-w-5xl">{cleanHeading(paper.sections_en[3].heading)}</h2>
      </div>
      <ul class="flex-1 flex flex-col gap-2 justify-center max-w-5xl">
        {extractBullets(paper.sections_en[3].body).map((b) => (
          <li class="takeaway-item">{b}</li>
        ))}
      </ul>
    </div>
  </section>

  <!-- Slide 9: Fig 3 algorithm generalization 3 panels — h-30 -->
  <section class="slide-page" data-page="8">    <div class="slide-top-accent"></div>
    <div class="slide-meta-bar">
      <span class="meta-page">08 / 13</span>
    </div>

    <div class="col-span-12 flex flex-col gap-5 h-full">
      <div>
        <p class="kicker mb-3">Figure 3 · Cross-Algorithm Generalization</p>
        <hr class="accent-bar mb-6" />
        <h2 class="text-headline font-sans font-bold leading-tight mb-4">PPO / GRPO / GSPO — Consistent Gains</h2>
      </div>
      <p class="caption flex-shrink-0">(a) MediQ intervention: outcome-RL reduces feedback sensitivity, increases belief consistency; (b) PE-F under GRPO; (c) FloDial-Easy under GSPO</p>
      <figure class="frame-img h-30 fit-contain">
        <img src={`${FIG}/fig3_full.png`} alt="Fig 3: algorithm generalization" />
      </figure>
    </div>
  </section>

  <!-- Slide 10: Q4 (2/2) -->
  <section class="slide-page" data-page="9">    <div class="slide-top-accent"></div>
    <div class="slide-meta-bar">
      <span class="meta-page">09 / 13</span>
    </div>

    <div class="col-span-12 flex flex-col gap-5 h-full">
      <div>
        <p class="kicker mb-3">Q4.2 · Experiments (Dynamics + Robustness + λ)</p>
        <hr class="accent-bar mb-6" />
        <h2 class="text-headline font-sans font-bold leading-tight max-w-5xl">{cleanHeading(paper.sections_en[4].heading)}</h2>
      </div>
      <ul class="flex-1 flex flex-col gap-2 justify-center max-w-5xl">
        {extractBullets(paper.sections_en[4].body).map((b) => (
          <li class="takeaway-item">{b}</li>
        ))}
      </ul>
    </div>
  </section>

  <!-- Slide 11: Fig 5 lambda + Fig 6 τ²-bench — dual h-30 -->
  <section class="slide-page" data-page="10">    <div class="slide-top-accent"></div>
    <div class="slide-meta-bar">
      <span class="meta-page">10 / 13</span>
    </div>

    <div class="col-span-12 flex flex-col gap-5 h-full">
      <div>
        <p class="kicker mb-3">Figures 5+6 · Robustness + Real Scenario</p>
        <hr class="accent-bar mb-6" />
        <h2 class="text-headline font-sans font-bold leading-tight mb-4">λ Reweighting Strength and Cross-Scenario Generalization</h2>
      </div>
      <p class="caption flex-shrink-0">Fig 5 (left): τ²-bench training dynamics — (a) rewards (b) tool execution errors (c) consumed response tokens (d) instruction turns · Fig 6 (right): τ²-bench AReW customer service</p>
      <div class="flex-1 grid grid-cols-2 gap-6 items-center">
        <figure class="frame-img h-30 fit-contain">
          <img src={`${FIG}/fig5_taubench.png`} alt="Fig 5: τ²-bench training dynamics — (a) rewards (b) tool execution errors (c) consumed response tokens (d) instruction turns" />
        </figure>
        <figure class="frame-img h-30 fit-contain">
          <img src={`${FIG}/gh-tau2-AREW.png`} alt="Fig 6: τ²-bench AReW customer service" />
        </figure>
      </div>
    </div>
  </section>

  <!-- Slide 12: Q5 -->
  <section class="slide-page" data-page="11">    <div class="slide-top-accent"></div>
    <div class="slide-meta-bar">
      <span class="meta-page">11 / 13</span>
    </div>

    <div class="col-span-12 flex flex-col gap-5 h-full">
      <div>
        <p class="kicker mb-3">Q5 · Future Work</p>
        <hr class="accent-bar mb-6" />
        <h2 class="text-headline font-sans font-bold leading-tight max-w-5xl">{cleanHeading(paper.sections_en[5].heading)}</h2>
      </div>
      <ul class="flex-1 flex flex-col gap-2 justify-center max-w-5xl">
        {extractBullets(paper.sections_en[5].body).map((b) => (
          <li class="takeaway-item">{b}</li>
        ))}
      </ul>
    </div>
  </section>

  <!-- Slide 13: Q6 -->
  <section class="slide-page" data-page="12">    <div class="slide-top-accent"></div>
    <div class="slide-meta-bar">
      <span class="meta-page">12 / 13</span>
    </div>

    <div class="col-span-12 flex flex-col gap-5 h-full">
      <div>
        <p class="kicker mb-3">Q6 · Summary</p>
        <hr class="accent-bar mb-6" />
        <h2 class="text-headline font-sans font-bold leading-tight max-w-5xl">{cleanHeading(paper.sections_en[6].heading)}</h2>
      </div>
      <ul class="flex-1 flex flex-col gap-2 justify-center max-w-5xl">
        {extractBullets(paper.sections_en[6].body).map((b) => (
          <li class="takeaway-item">{b}</li>
        ))}
      </ul>
    </div>
  </section>

  <!-- Slide 14: Q7 (restored) -->
  <section class="slide-page" data-page="13">    <div class="slide-top-accent"></div>
    <div class="slide-meta-bar">
      <span class="meta-page">13 / 13</span>
    </div>

    <div class="col-span-12 flex flex-col gap-5 h-full">
      <div>
        <p class="kicker mb-3">Q7 · Learn More</p>
        <hr class="accent-bar mb-6" />
        <h2 class="text-headline font-sans font-bold leading-tight max-w-5xl">{cleanHeading(paper.sections_en[7].heading)}</h2>
      </div>
      <p class="qa-body flex-1 whitespace-pre-line">{paper.sections_en[7].body}</p>
    </div>
  </section>

  <!-- Slide 15 (Q7 restorable) -->
</BaseLayout>
